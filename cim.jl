#=---------------------------------------------------------
cim.jl


---------------------------------------------------------=#

function beyn(D, L)
    # ComplexF64 are aliases for Complex{Float64}
    A0 = zeros(ComplexF64, D, L)
    A1 = zeros(ComplexF64, D, L)
end